const Loading = () => {
    // You can add any UI inside loading, including a Skeleton. 
    return <_LoadingSkeleton /> // A spinner, or an example of the page, when the page is loading. 
}

export default Loading;