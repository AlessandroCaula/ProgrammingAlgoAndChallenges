import React from 'react'

// Loading component, that will be shown through all the routes, when the page is reloading.

const loading = () => {
  return (
    <div>____ loading _____</div>
  )
}

export default loading